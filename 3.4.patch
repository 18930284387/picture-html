diff --git a/src/App.tsx b/src/App.tsx
index 9601cbd..d9a4695 100755
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -41,6 +41,7 @@ interface Photo {
   comments: Comment[];
   tags: string[];
   isLiked: boolean;
+  filterClass?: string;
 }
 
 interface Comment {
@@ -188,6 +189,17 @@ export default function App() {
   const [uploadTitle, setUploadTitle] = useState('');
   const [uploadTags, setUploadTags] = useState('');
   const [uploadPrivacy, setUploadPrivacy] = useState<'public' | 'followers' | 'private'>('public');
+  const [selectedFilter, setSelectedFilter] = useState<string>('');
+  
+  const filters = [
+    { class: '', label: '原图' },
+    { class: 'grayscale', label: '黑白' },
+    { class: 'sepia', label: '复古' },
+    { class: 'invert', label: '反色' },
+    { class: 'blur-sm', label: '模糊' },
+    { class: 'saturate-200', label: '饱和' },
+    { class: 'contrast-150', label: '对比' }
+  ];
   
   const [settingsName, setSettingsName] = useState('Alex Chen');
   const [settingsBio, setSettingsBio] = useState('Photography enthusiast from San Francisco');
@@ -284,12 +296,14 @@ export default function App() {
       likes: 0,
       comments: [],
       tags: uploadTags.split(',').map(t => t.trim()),
-      isLiked: false
+      isLiked: false,
+      filterClass: selectedFilter
     };
     setPhotos([newPhoto, ...photos]);
     setUploadPreview('');
     setUploadTitle('');
     setUploadTags('');
+    setSelectedFilter('');
     setCurrentPage('home');
   };
 
@@ -454,7 +468,7 @@ export default function App() {
             <img
               src={selectedPhoto.url}
               alt={selectedPhoto.title}
-              className="w-full h-full object-contain"
+              className={`w-full h-full object-contain ${selectedPhoto.filterClass || ''}`}
             />
           </div>
 
@@ -636,10 +650,14 @@ export default function App() {
             >
               {uploadPreview ? (
                 <div className="relative group w-full h-full">
-                  <img src={uploadPreview} alt="Preview" className="w-full h-full object-cover" />
+                  <img 
+                    src={uploadPreview} 
+                    alt="Preview" 
+                    className={`w-full h-full object-cover transition-all duration-300 ${selectedFilter}`} 
+                  />
                   <div className="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                     <button
-                      onClick={(e) => { e.stopPropagation(); setUploadPreview(''); }}
+                      onClick={(e) => { e.stopPropagation(); setUploadPreview(''); setSelectedFilter(''); }}
                       className="p-3 bg-white/95 rounded-full shadow-lg transform hover:scale-110 transition-all"
                     >
                       <Trash2 className="w-6 h-6 text-gray-700" />
@@ -665,6 +683,37 @@ export default function App() {
               />
             </div>
 
+            {uploadPreview && (
+              <div className="px-6 pt-4">
+                <div className="overflow-x-auto">
+                  <div className="flex gap-3 pb-2">
+                    {filters.map((filter) => (
+                      <button
+                        key={filter.label}
+                        onClick={() => setSelectedFilter(filter.class)}
+                        className={`flex-shrink-0 flex flex-col items-center gap-2 p-3 rounded-2xl transition-all duration-300 ${
+                          selectedFilter === filter.class
+                            ? `border-3 border-teal-500 ${darkMode ? 'bg-gray-800' : 'bg-gradient-to-br from-teal-50 to-cyan-50'} shadow-md`
+                            : `border-2 ${borderColor} ${hoverBg}`
+                        }`}
+                      >
+                        <div className={`w-12 h-12 rounded-xl overflow-hidden ${filter.class}`}>
+                          <img 
+                            src={uploadPreview} 
+                            alt={filter.label}
+                            className="w-full h-full object-cover"
+                          />
+                        </div>
+                        <span className={`text-sm font-semibold ${selectedFilter === filter.class ? 'text-teal-600 dark:text-teal-400' : textPrimary}`}>
+                          {filter.label}
+                        </span>
+                      </button>
+                    ))}
+                  </div>
+                </div>
+              </div>
+            )}
+
             <div className="p-8 space-y-6">
               <div>
                 <label className={`block text-sm font-semibold ${textPrimary} mb-3`}>Title</label>
@@ -857,7 +906,7 @@ export default function App() {
                       className="group relative aspect-square overflow-hidden rounded-2xl cursor-pointer shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-1"
                       onClick={() => setSelectedPhoto(photo)}
                     >
-                      <img src={photo.url} alt={photo.title} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
+                      <img src={photo.url} alt={photo.title} className={`w-full h-full object-cover group-hover:scale-110 transition-transform duration-500 ${photo.filterClass || ''}`} />
                       <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4">
                         <div className="flex items-center gap-6 text-white">
                           <span className="flex items-center gap-2 font-semibold text-lg">
@@ -878,7 +927,7 @@ export default function App() {
                       className="group relative aspect-square overflow-hidden rounded-2xl cursor-pointer shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-1"
                       onClick={() => setSelectedPhoto(photo)}
                     >
-                      <img src={photo.url} alt={photo.title} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
+                      <img src={photo.url} alt={photo.title} className={`w-full h-full object-cover group-hover:scale-110 transition-transform duration-500 ${photo.filterClass || ''}`} />
                       <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4">
                         <div className="flex items-center gap-6 text-white">
                           <span className="flex items-center gap-2 font-semibold text-lg">
@@ -904,7 +953,7 @@ export default function App() {
                       className="group relative aspect-square overflow-hidden rounded-2xl cursor-pointer shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-1"
                       onClick={() => setSelectedPhoto(photo)}
                     >
-                      <img src={photo.url} alt={photo.title} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
+                      <img src={photo.url} alt={photo.title} className={`w-full h-full object-cover group-hover:scale-110 transition-transform duration-500 ${photo.filterClass || ''}`} />
                       <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4">
                         <div className="flex items-center gap-6 text-white">
                           <span className="flex items-center gap-2 font-semibold text-lg">
@@ -1333,7 +1382,7 @@ export default function App() {
                     <img
                       src={photo.url}
                       alt={photo.title}
-                      className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
+                      className={`w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 ${photo.filterClass || ''}`}
                       loading="lazy"
                     />
                     <div className="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
